M2（端到端数据流转与跨界集成） 阶段。

请注意执行顺序：你必须先下发给 Agent-Spec (A1)，等它在 TASK_BOARD.md 汇报完成后，再把剩下的 4 个 Prompt 并发给另外 4 个 Agent。

以下是为你准备好的、可以直接复制粘贴给 OpenClaw 的标准 Prompt 文本：

1. 第一步：下发给 Agent-Spec (A1) —— 制定通信契约 (Blocker 任务)
(等待 A1 汇报完成后，将以下 4 个任务同时下发)
2. 第二步并发：下发给 Agent-Rust (A2) —— 开启内核 HTTP 监听
3. 第二步并发：下发给 Agent-SDK (A5) —— 打通客户端发送逻辑
4. 第二步并发：下发给 Agent-Phys (A4) —— 物理模拟器微服务化
5. 第二步并发：下发给 Agent-LLVM (A3) —— 构建 LLVM 语法树解析器
总指挥，你的下一步动作：
把第一条指令发给 A1，等它跑完，迅速把剩下四条扔进各自的 OpenClaw 终端。如果期间有任何一个 Agent 报错“找不到文件”或“依赖冲突”，直接把它的 Error Log 丢给它自己，让它自行